working today on the hackathon.

list the types in a module.
show the relationship between types.
create a switch statement to collect the co-occurences of all the types as
a count in a matrix.
collect the types of fields that relate the types.
use a different matrix for each field.
